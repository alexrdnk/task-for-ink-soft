spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://dpg-d15agph5pdvs73f39odg-a.frankfurt-postgres.render.com:5432/localnews?user=localnews&password=JF0ju86oSqedZ8S0AiTTkgnXA0kIyNKC&sslmode=require&connectTimeout=30&socketTimeout=30&tcpKeepAlive=true&reWriteBatchedInserts=true
    username: localnews
    password: JF0ju86oSqedZ8S0AiTTkgnXA0kIyNKC
    hikari:
      connection-timeout: 30000
      maximum-pool-size: 5
      minimum-idle: 1
      idle-timeout: 300000
      max-lifetime: 1200000
      connection-test-query: SELECT 1
      validation-timeout: 5000
      keepalive-time: 30000
      leak-detection-threshold: 60000
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        connection:
          provider_disables_autocommit: true
        jdbc:
          batch_size: 50
          batch_versioned_data: true
  webclient:
    base-url: https://newsapi.org
  retry:
    max-attempts: 3
    initial-interval: 1000
    multiplier: 2.0
    max-interval: 10000

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always
  server:
    port: 8080
    base-path: /actuator

logging:
  level:
    root: INFO
    com.zaxxer.hikari: DEBUG
    org.springframework.jdbc.datasource: DEBUG
    org.hibernate.SQL: DEBUG
    org.springframework.retry: DEBUG
    org.postgresql: DEBUG
    org.hibernate.engine.jdbc: DEBUG

server:
  port: ${PORT:8080}
  address: 0.0.0.0
  tomcat:
    connection-timeout: 20000
    max-threads: 200
    min-spare-threads: 10
    threads:
      max: 200
      min-spare: 10
  shutdown: graceful
  error:
    include-message: always
    include-binding-errors: always

newsapi:
  base-url: https://newsapi.org
  key: ${NEWSAPI_KEY}

openai:
  api-key: ${OPENAI_API_KEY} 